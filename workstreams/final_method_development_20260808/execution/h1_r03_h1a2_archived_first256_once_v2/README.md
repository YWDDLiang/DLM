# Fast registered-identity launcher

This immutable repair preserves the v1 scientific contract and exact archived
body/refiner/evaluator code. It replaces repeated full reads of the 6.39 GB B0
adapter and model_494 during preflight with their registered SHA-256 identities
plus frozen path, byte-size, and modification-time checks. The compute job still
loads the checkpoints normally and the fresh body outputs must pass the exact
historical byte-SHA gate before refinement.
