"""CrysLLMGen-derived implementation layer for the active Wyckoff method.

The byte-preserved source snapshot lives in :mod:`.upstream`.  New runtime
code must import or port from that snapshot with an explicit parity test; the
snapshot itself is not edited.
"""

