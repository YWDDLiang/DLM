# 下一步最小对照规格

状态：复盘后准备的执行规格；**本次没有提交GPU或模型重跑**。目标是把“历史强点未被保留”拆成可验证的问题，禁止用旧达标cohort代替新的确认实验。

## A. 同一结构的评价桥接

输入固定为本次已逐行核验的归档first256 H1-A2/D1和R03/D2；对应原terminal SHA为`63128c86b9ba0f1688c3f8db543512e6c19d5a68bbabd6dc538f10c1b79f85b0`。来源、512条attempt以及28/128的计数在[账本](HISTORICAL_RESULT_LEDGER.json)中。使用它们只做历史机制定位，不当新cohort成功。

1. 取得原评价输入结构，逐项绑定原manifest、CIF/张量、site identity、晶格/分数坐标。不能仅凭两个目录都写“first256”认定输入相同；例如两个归档评价视图的N∩U已有224与227的差别，须继续保留各自artifact identity。
2. 保持256请求全分母、原N/U布尔值和同一官方hull快照，复用旧R能量，另跑当前显式固定R。没有Planner、DLM、model494新采样或候选选择。
3. 逐请求报告旧/新能量、是否获得有限值、R状态、Strict/Meta翻转、known/unknown。再把当前N/U算法应用于**同一输入**作独立记账检查；不要同时改变结构和N/U再把总差归到R。
4. 缺原结构、site映射或hull输入时明确停止该格，不补造历史R的未记录优化器状态/力/应力，也不把新状态回填为旧状态。

这个对照只回答旧分数在当前测量链上怎样变化。它不证明某个训练初始化更好。

## B. 保护强基线的前瞻对照

保留H1-A2/B0+D1、H1-A2/B0+D2两条可追溯历史控制，以及当前K4完整循环。首先验证各自原tokenizer、adapter、采样日程和输入ABI；不能把不同词表/Plan语义的权重直接拼起来。旧训练runtime commit未记录的部分继续注明恢复源码身份。

整体系统比较用预先冻结的新de novo请求及完整分母，分别报告生成分布与最终SUN。若要归因DLM，则另建固定C/N的共同支持panel，并把rich/soft/program条件的转换写成独立因子；不能把不兼容的SG/volume字段偷偷argmax映射后称为同条件，也不能删掉不支持的组成。

每条均报告refiner前与固定tau800后；相同R、N/U参考、hull快照和预先声明的随机/计算预算。独立重复要重新抽Planner cohort；同一cohort的CUDA重复另列。

## C. 学习方向只留下一个可判别因素

优先比较实际完整程序的价值定义，而非再改多个模块：原生目标使用完整DLM→R的回报，系统tau800目标则明确包含model494→R。保持同一初始checkpoint、数据来源和计算预算，先测学生实际rollout是否学到该目标。

保留严格teacher验证；扩大数据时同时报告有多条verified候选的组成覆盖、teacher重加权幅度与学生兑现程度，不把总path数当作同等有效的物理对比信号。Potential-Closure的方向值得保留，但其当前256结果和原生能量区间不足以宣布胜利。

预检只负责证明实现、来源和数值合同成立；最终采用与否仍依据预先冻结的独立生成结果。旧105/488聚合headline、gold条件高分、最佳单个process及survivor-adjusted结果均不能作为新方法的确认数据。
