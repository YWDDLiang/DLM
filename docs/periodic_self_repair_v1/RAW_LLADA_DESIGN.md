# Raw-initialized periodic numerical DLM: completed raw-v1 contract

Status updated 2026-09-06. **This is the historical contract for the completed raw-v1 run, not the current execution plan.** Train39942 finished two epochs and 6784 updates; native39975 and tau80039984 completed the fixed 256 evaluation, with Strict/Meta SUN 3/45 and 16/113. Neither endpoint triggered the 26/128 threshold for an additional 1000. Current audit, reviewed V3 work and authorization are maintained in [CURRENT_STATE](../CURRENT_STATE.md); historical pause language in this contract does not override them.

This raw-v1 version replaced the then-proposed K4/K8 warm start. It loaded only the original LLaDA-8B-Instruct and tokenizer. Fresh crystal tokens had independent trainable FP32 input/output row increments; original vocabulary rows stayed frozen. LoRA used the raw initializer's r8/alpha32 q/k/v/ff/up configuration. Registered adapter fields and the active training policy must be read from the archived initializer and run configuration, rather than inferred from an older reference recipe.

Preserve the original Planner, species program, exact composition/7+4N canvas and geometric legal logits. Numeric-logit increments use eight Fourier modes for coordinates, log-length/RBF for lengths and ordered nonperiodic RBF for angles. A zero-initialized MLP supplies actual old periodic pair geometry directly as Transformer attention bias. Unknown coordinates cannot create geometry pairs.

Task/mask/noise are explicit; unknown error is -1, regardless of visibility. Clean construction uses its masked canvas as old geometry, preventing target leakage. Repair uses a structured corruption and its correctly conditioned clean prefix. Known-noise metadata is hidden with probability0.5 to cover the deployment unknown-noise condition.

The run read only original-MP20 teacher-only train/val data (27136/9047 sources), with prompt, clean answer, Plan/program and source metadata. Each source had a dense random-mask construction view and a scalar full-cell repair view per epoch: two views, two complete epochs, 108544 source-view examples, global batch 16 and 6784 updates. No K4/K8 generated paths, labels, weights or previous DLM tables were used.

Loss is family-balanced schema denoising CE plus0.25 times a sampled actual-legal-support CE. Each contains90% exact CE and10% numerical CE, narrowing from0.75 to0.25 bins. Schema temperature is1; legal-policy temperature is0.7, with the same alias folding and raw dtype as deployment. An unsupported teacher remains in schema training; its legal contribution is zero and the conflict is recorded. Targets are never added back to legal support.

Dense CE is conditional denoising, not full trajectory likelihood. Raw-model deployment uses scalar construction followed by full-cell repair and whole-transaction rollback. Construction conditions all remaining masked numeric positions consistently with training and trace replay. Older half-cell/cooperative/closure phases remain optional legacy interfaces.

Reproduction entrypoints are [240 train](../../slurm/240_train_periodic_dlm_from_base.sbatch) and its [trainer](../../src/scripts/train_periodic_dlm_from_base.py), followed by [241 native](../../slurm/241_evaluate_periodic_dlm_native.sbatch) and [242 tau800](../../slurm/242_evaluate_periodic_dlm_tau800.sbatch). The retained training entrypoint explicitly passes microbatch 2 on two ranks; its global-16 rule yields accumulation 4. The previous microbatch1/accumulation8 paragraph described an older reference recipe and is not the current entrypoint. For the exact executed configuration, use train39942's archived code, CODE_COMMIT and training_config.json; do not infer unverified run hyperparameters or runtime from canonical153/39282. The confirmed accounting is two views per source per epoch, two epochs and 6784 global-16 updates.

Integrated checks covered raw initialization, new-row/module gradients, finite loss and complete source/view coverage. The same 100 original validation sources monitored both epoch boundaries without checkpoint selection. The completed fixed 256 native and frozen model494 tau800 endpoints remain separate; their results and hashes are in the [evaluation evidence](../v3_scientific_audit_20260906/evidence/EVALUATION_SUMMARIES_20260906.json). Periodic features use the declared 125-image shell; strict whole-model equivariance and universally exact nearest images are not claimed.

The raw-v1 contract did not include own-error collection, K4/K8 continuation or draft job243. That historical scope does not pause the current scientific audit or define the authorization for a separately reviewed V3. This completed version is not resubmitted by refreshing its documentation. The original full bytes and SHA256 were preserved in the [status-refresh archive](../v3_scientific_audit_20260906/historical/current_entrypoints_before_status_refresh/manifest_aa5d826c7e39cd442817d49c7605444332b95a5fbe5f45c28b80f7fead43da78.json).
