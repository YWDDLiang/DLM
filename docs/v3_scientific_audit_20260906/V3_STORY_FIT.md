# H-P33 与原论文故事的契合边界

2026-09-06。用户提出“v3会不会和我们之前想的故事不搭？”。**若原核心是“LLM生成结构程序，离散DLM按程序执行”，当前H-G确实偏离；共享骨干和真实梯度不足以关闭这一缺口。** 本页明确论文定位，不改正在运行的40064科学合同，也不预设其SUN。

用户随后明确：**效果优先，论文为重点。** 若V3有效，允许围绕准确的LLM+DLM科学问题重组故事；若raw/refined缺乏可信改善，则回到K4/K8或此前有效路径，选择稳定性改进并实际应用。因此本页用于限制不成立的主张，保留有效H-G成为主线的可能。是否达到1000补评门槛与是否有相对改善是两个判断，不用单一阈值代替配对结果。

| 环节 | 原较强叙事 | 当前H-G实际行为 |
|---|---|---|
| C³FD / LLM | 化学支持、组成、软计划与P | 保留冻结上游及同样条件 |
| P | 控制几何token生成顺序/交易执行 | G中主要是条件与rank，不控制连续更新顺序 |
| 离散DLM | 直接决定生成的几何token | T只作辅助训练；G采样不调用词表输出 |
| 几何生成 | 离散状态上的条件生成/修订 | 共享LLaDA隐状态上的连续v/u预测与H-P33积分 |
| refiner | 独立后处理、单独计分 | 保留原model494 tau800及共同评测 |

H-G可以被准确描述为“语言模型骨干的连续晶体几何扩展”或“共享语言骨干的混合任务生成模型”。它不能直接证明离散扩散采样、程序执行或P规划优于更简单替代。把输入排成token序列、保留一个T损失，或在名称中保留DLM，都不能替代部署行为与贡献证据。

此前主审以数学自洽、骨干共享、真实梯度判断它值得有限实验，对原故事契合度的要求放得偏宽。两轮审查实际关闭的是定义/实现阻断，不是论文主张已经成立。即使H-G提升SUN，额外两epoch、连续监督、表示与solver同时改变，也不能归因于语言预训练、T辅助或P中的某一项。

## 更贴原故事的可检验用法

同一最终权重保留T construction分支。连续几何任务可以作为辅助学习信号，部署仍由T按既有程序生成离散token；若这一离散端点改善，才有机会支持“几何监督帮助DLM”的较贴切叙事。当前只是可行性方向，尚未注册新增采样或宣称收益；接口与对照正做有界核对，无须因此重新训练。

合理对照至少包含同口径的V2 construction raw（已测12/44，256请求）。若检验construction→tau800，应补齐V2同一construction终点的tau800对照；该项此前未测，不能用V2 repair→tau800的16/113冒充。P、T辅助和语言预训练各自的必要性仍需相应证据，单一端点提升不足以替代它们。

当前40064继续原已授权实验，完整终点才运行原注册的H-G float raw/refined与同源Q诊断。论文采用同时考察实际SUN和核心贡献是否仍成立，不把更好分数自动等同于可替换原主线，也不在看到分数后改称这是纯离散DLM收益。

论文首先提出一个统一、可检验的科学问题，说明what/why/how，再组织2–3个互补方法贡献。每项应解决同一问题中的不同困难，并对应明确机制、最近已有工作和必要实验；不把组件数量、工程检查或固定第三方refiner当创新数量。两条结果分支共用这一约束。
