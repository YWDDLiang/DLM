# Plan1200 tau800 scale validation

This profile evaluates the complete frozen C3FD–Llama → Compact-V2/G2 →
model494 pipeline at scale while keeping the paper headline at Strict/Meta
S.U.N. `105/488` per 1,000.

## Protocol

- The fused Planner produces `1200/1200` composition-valid Plans.
- Official references are available for 1,159 requests.
- The DLM produces 1,139 valid CIFs; 20 CIF construction failures are disclosed.
- The scale main1000 contains the first 861 valid rows from the original main
  block plus all 139 valid rows from the remainder block.
- Each request uses one Plan, one DLM trajectory and model494 tau800. No retry,
  replacement, reranking or additional official query is used.

## Result

| Denominator | N | U | N∩U | Strict S.U.N. | Meta S.U.N. | Mean hull | Median hull |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1,000 | 872 | 987 | 866 | **81/1000 = 8.10%** | **486/1000 = 48.60%** | 0.1022 eV/atom | 0.0669 eV/atom |

The independent scale profile confirms high composition/CIF execution and a
Meta S.U.N. level consistent with the main reported range. The Strict variation
is treated as sampling variability rather than a replacement headline.

Runs: body generation `39183`, model494 tau800 `39199`, cached evaluation
`39204`, final repartition evaluation `39205`.
