# 固定G/F的KEEP/EDIT尝试与后续1000条执行计划

状态：执行中。2026-09-10（北京时间）用户最新要求：成功和失败都要详细论证；本次尝试结束后不再大改，只修小bug；后续raw 1000使用干净数据训练；先写计划，完成后更新main.md及相关记录；优先目标收敛为Stable和SUN。

## 已完成且保留的工作

- 原256条S0＋三次G/E更新已完整结束；固定其G3/F输出作为本次E比较输入。
- 本次原始登记为`PREREGISTRATION.json`，153/51/52个来源分别用于训练/开发/最终验证，按reduced_formula分组。最终验证未进入本次E更新。
- 已编译3294条候选判断、670个不同current；包含52个Stable提升内容目标、85个Stable恒等保护目标。所有目标来自同Plan、已评分的实际token结构。
- 17项CPU检查和6卡真实模型短验证通过。`2e-6`内容学习率的模型完成2次完整内容更新后触发KL；`2e-7`的预登记尝试和只训练决策头对照继续按原上限执行。
- 原始试验12小时预算从2026-09-09 19:51:18 UTC计至2026-09-10 07:51:18 UTC，最多6张GPU、3个Slurm作业；诊断和修正不重置该截止时间。

## 接下来的顺序与完成标准

1. **诊断KL约束。** 先绑定触发批次、每张卡的内容样本和质量类别；对冻结的已训练模型测量全部训练内容目标在T2T/M2T两种视图上的参考KL、目标CE变化及分位数。分别报告全局均值、尾部和Stable提升/保护组，不能将最大卡小批次均值冒充整个数据集的偏移。先记录证据，再决定是否需要有界的KL统计或阈值校正。
2. **仅完成当前有界尝试。** 保留现有模型结构、固定G/F、可信目标数据、完整遍历内容更新和独立头训练。若KL诊断确实支持调整，必须先写带时间戳的登记补充，说明唯一改动、对照、上限和选择规则，再运行；不通过继续缩小学习率来假装学会，也不靠训练损失宣称有效。最终验证仍不参与模型或阈值选择。
3. **固定输入比较与成功/失败论证。** 比较同输入KEEP、旧E3、只训练决策头、本次T2T模型。先在开发来源选择有限的KEEP/EDIT决策组合，再冻结选择，最后读取最终验证指标。主判断只看Stable和SUN：Stable应超过KEEP及旧E3，SUN不低于两者，并核查已有Stable的损失。记录内容目标实际更新覆盖；覆盖不足或仅训练集变好不能当成成功。逐条分解Stable/SUN获得、保留、损失，以及好提案被拒绝、坏提案被接受；同时说明小样本和旧RSI已见来源的限制。
4. **冻结方法并执行1000条。** 本次主判断成立则采用通过比较的方案，否则沿用已完整跑通的旧方法。结果确定后不再调整大方法、模型结构或评价口径，仅修复阻断执行的小bug。1000条仍为S0＋三次G/E更新；从登记B0及新建E初始化开始，不能把本次E试验权重、优化器或验证标签直接带入。启动前核验TRAIN来源、固定哈希抽样、ancestor/重复样本、MAIN验证组成隔离、目标与物理标签绑定、训练/验证角色和每轮实际内容更新。1000条是用户确认的TRAIN更新面板，不冒称独立测试集。
5. **归档和文档。** 保留所有尝试、失败回执、配置、数据/代码哈希、资源使用和完整分母；更新`main.md`、`EXPERT_REPAIR_DISCUSSION.md`、实验结果与方法说明。清楚记录采用或回退理由，区分实际对照支持的结论与推测，并报告1000条每一轮真实结果。

## 最新范围调整

用户明确只优先Stable和SUN，因此MSUN不再作为本次成败的强制门槛，尚未启动的多候选N/U扩展不作为必做工作。已有MSUN、N/U字段可保留在常规记账中，只有解释Stable/SUN变化必需时才分析，不扩展新目标。原始登记文件和已经执行的配置保持不变；最新范围用单独补充记录，不覆盖历史。

## 待完成回执

2026-09-10约04:33事前补充（最终验证尚未参与任何选择）：全量632目标诊断显示，原学习率模型T2T KL均值0.023775确实超过0.02，M2T均值0.004118；Stable提升目标T2T平均CE下降0.117865，Stable保护目标下降0.110622。低十倍学习率、8遍历模型分别仅下降0.047776和0.045095。不能把KL当作质量指标，也不能据CE直接宣称Stable/SUN提高。

据此只增加**一次关闭硬停的对照**：从同一旧E3起点开始，内容学习率2e-6、头1e-4、相同数据/顺序/种子、软KL系数1和8遍历/3600秒上限全部不变；仅关闭超过0.02后停止内容更新的行为，仍记录每次越界和最大KL。它与已经完成的原硬停、低学习率、只训练头三组共同形成四个固定候选，不再添加更多训练设置。必要时用同GPU分片与批次重新执行旧E3作为数值一致性对照，不更新其权重。

选择规则事先固定为四个已登记候选各自最多6个决策组合，共24个开发集比较；按Stable、SUN、较少Stable损失、较少实际编辑、较高接受阈值、尊重KEEP排序，完全相同时优先较少内容更新。最终一次冻结权重与决策组合，最终验证只决定采用/回退，不再选另一组权重。各组在共同决策组合下的结果用于归因，不能把独立调过的阈值差异归为KL的单独作用。

按用户最新“只优先Stable/SUN”的要求，八遍历是本次训练目标及上限，不能用遍历数代替实际效果。训练完整性检查要求每条判断监督均完成8次头更新；内容更新的候选至少完整覆盖一次T2T和一次M2T，并报告每个Stable提升目标的实际覆盖；只训练头候选如被开发集选择，明确称为头方案，不冒称内容修复学会。成败仍由最终验证Stable超过KEEP及旧E3、SUN不低于两者、已有Stable损失不高于原对照（最多1条）决定。其它常规字段仅作记录。这些变更保留在独立登记补充中，不覆盖原始规则。

2026-09-10实施前小bug修复计划：对旧E3决策的256条重放检查发现，复制到试验目录的反馈清单仍声明原输入路径，严格绑定检查因此拒绝。修复限定为：先验证原反馈清单、实际输入字节和完整物理标签一致，再为完全相同的复制输入建立新的路径清单及来源范围绑定，并保留原报告哈希；不更改结构、标签值或评价协议。随后重放旧E3，要求256条最终token逐条相同且物理重绑定通过，才允许正式比较。

2026-09-10北京时间约05:12，评分前小bug处理计划：首次两组提案标注在计算开始前被缓存身份检查拒绝。检查显示joint-stop身份绑定整个ranked_feedback.py文件，而本次在该文件加入了与物理停止无关的训练标签归一化函数。先暂停调度进程继续提交失败标注，保留正在完成的提案作业及全部权重；只读比较旧/新物理模型、包、标注器和停止函数，确定真实差异。若差异仅为文件中非物理代码，则将本次试验的物理标注显式绑定到旧对照的不可变源码快照，并保留严格身份验证，不豁免哈希检查、不重写旧标签身份。新编辑器仍使用已冻结源码；用CPU缓存全量核验确认旧物理快照的实际身份逐项一致后，仅重试未完成标注/评分，保存新的作业回执和原失败日志。若物理函数或模型实际不同，则不能按此方案复用，必须先记录差异再选择同口径重新标注。该修正不改变训练、提案token、选择规则或绝对截止时间。

同次恢复补充：新调度源码路径使前两组已完成的本地materialize命令回执与新命令路径不一致。停止旧恢复调度进程但让已启动的有效标注作业完成；保留原两个完成回执副本，以新回执重新执行确定性的JSON组装。要求inputs.jsonl、FEEDBACK_MANIFEST.json、MATERIALIZED.json前后SHA256全部相同，才继续；不重新生成提案，也不更改旧回执中的执行命令。后续统一从同一恢复源码继续。

2026-09-10北京时间约05:41，组装面板小bug修复计划：五组提案标注与评分均已完成，但旧E3匹配策略面板在标签重绑定时拒绝复制路径。原因是二次复制连同上一层COPIED_STAGE_BINDING标记一起复制，函数只核对输入哈希就误判已为新目录绑定。修复限定为：标记同时绑定本次source/destination输入路径；仅当路径、输入哈希及实际反馈/物理标签验证均通过时复用。继承的旧标记不得提前返回。用真实旧E3材料做连续两次复制的CPU验证，并恢复已有部分策略面板的元数据绑定与标签重绑定；其inputs字节必须不变，完成后续行。恢复调度保留已完成科学命令原e3b7037路径，仅面板组装代码使用修复快照，避免再次改变本地完成命令路径。所有模型、提案、物理标签值、阈值网格、选择规则和期限不变。

- [x] KL触发来源及全量KL/CE诊断，632目标×2种视图×3组；见[证据与结果](EDITOR_T2T_TRIAL_RESULTS_20260910.md)
- [x] 必要时的事前登记补充与有界验证；最后关闭硬停对照完成8次内容和552次头更新，无超时，不再增加训练配置
- [x] 决策组合冻结及最终Stable/SUN比较；heads、尊重KEEP、接受阈值0.8，FINAL的Stable/SUN为5/5，与两对照持平，未通过采用条件
- [x] 成功或失败详细论证与方法冻结；全部KEEP、动作范围扩大、Stable提案不足及KL对照已归档，冻结回退旧方法
- [x] 1000条干净数据及初始化检查；正式启动时再次检查冻结配方和初始化来源
- [ ] 1000条S0＋三次G/E更新
- [ ] main.md及相关文档同步

## 1000条采用旧方法的具体执行计划（试验判定后、启动前）

固定G/F试验已完整结束，主判定为失败：四个候选在共同冻结策略下均实际全部KEEP，整256条Stable/SUN为32/28，原E3为33/32；封存52条的5/5未超过KEEP及旧E3。最终验证不用于重新挑选其它候选或修改阈值，不增加训练设置。后续按用户原约定沿用已完成256全流程的方法。

启动前保存1000条原准备配置的副本，再登记冻结配置与输入哈希：保留1000个TRAIN ancestor、原256完整Plan/prompt/seed前缀、MAIN组成零重叠、G从B0和E新初始化。关闭试验性的历史Stable教师替换、单current模式归一化、KL触发后头续训及稠密T2T路径；继续使用旧候选级监督、旧偏好目标和旧KL停止规则。E运行仍采用原非SUN提案规则、0.5接受阈值、已知SUN的KEEP prior=9及单次E预算。G/E学习率、固定槽位/几何支持、F800、共同物理停止规则均沿用旧方法。

执行依次为S0（含独立第二RAW偏好对照与新E0初始化）和三次真实G/E更新S1—S3，每一轮走完draft→refine→token→Edit/KEEP再判断。训练只使用本1000运行新生成、同Plan且绑定完整协议的反馈；不导入此次试验权重、优化器、DEV/FINAL标签或旧运行端点缓存，固定物理参考库保持原定义。继续按已登记吞吐设置最多6张A800、每卡4CPU、单路6卡/并行3+3、训练每卡batch32、最多3个作业，正式调度起计12小时绝对期限；不重置旧试验期限。训练依剩余完整面板预算分配，保留G4/E8遍历上限和原KL规则，如实记录提前停止与实际监督覆盖，不以检查过的数据冒充已更新。

开始前重新核对三个运行配置、固定样本、全新初始化位置以及源码身份；运行过程中只修阻断执行的小bug，不根据某轮指标重采样或改大方法。更新main.md、试验结果/讨论及1000逐轮回执，分别说明G/F变化、E的Stable/SUN获得与损失及失败分母。

执行回执：1000条已于北京时间2026-09-10 06:01:15正式调度，冻结源码657985c0e501f629a4eb52f4e14d2404b60bf2c6，S0主路41745与第二RAW对照41746各3卡开始生成；绝对截止18:01:15。[启动回执](receipts/CLEAN_1000_FROZEN_METHOD_AND_LAUNCH_20260910.json)保存最终配置、样本哈希、新初始化核验及独立预算。

2026-09-10北京时间约07:09，1000条评分缓存修复计划（实施前）：两路生成和共同物理标注均完整结束，但评分作业41756/41757因官方参考缓存缺少部分扩展面板化学体系而退出。先保留失败组件、提交回执和调度失败记录，并核对缓存构建方式及原冻结参考来源；仅从同一参考来源补齐本1000个固定Plan所需的体系覆盖，核对旧有缓存内容不变。不得将缺失体系删出分母、豁免覆盖检查或把未知当作不稳定/Stable。补齐前核实参考身份，补齐后验证所有1000个Plan及两路实际输入都已覆盖，原输入、物理标签、配置和源码哈希保持一致。只归档未完成评分组件并登记新评分作业名；恢复调度显式保持657985c源码、原12小时绝对截止及完成S0＋三次G/E更新的顺序。若无法从同一冻结参考来源补齐，先记录该事实，不用改变参考口径来掩盖缺口。

同次修复的预防措施（代码实施前）：在调度入口增加按完整固定Plan检查参考缓存覆盖的步骤，缓存必须有完整来源与计数记录；真正的官方参考缺失和传输失败继续区分。覆盖不足应在任何新GPU提交之前明确报错。此检查不补写物理标签、不替换Plan、不改评分规则；用本次真实缺失缓存确认能拦截，再用补齐后的缓存确认能通过。本轮实际生成、标注、训练和评分继续使用已冻结源码快照，预防检查作为调度层修正单独登记。

缓存启用方式补充（启用前）：扩展缓存已由原工具验证为同一2026.04.13版本，925体系全覆盖（895可评分、30官方参考缺失、0传输错误）。为保留来源清单中的原路径，不覆盖旧缓存；将扩展目录、完成清单哈希、Plan哈希和预期数据库版本登记为单独引用。调度检查核对该引用，评分任务通过既有`--cache`参数选择扩展缓存；原三份RUN_SPEC及已完成输入、标签、源码保持字节不变。该参数仅改变参考文件位置与所需覆盖范围，不改热力学筛选、数值或评分规则。新增引用的路径、哈希和版本检查通过后再恢复调度。

2026-09-10北京时间约07:51，S0排队分配调整计划（实施前）：节点8卡中已分配6卡，仅有2卡空闲；对照F800的3卡作业41764因Resources等待超过10分钟。仅暂停本轮调度进程，保留正在运行的主路F800；只有41764仍为PENDING时才取消它，确认没有实际计算后以新作业名用2卡运行。S0剩余对照F800及标注采用2卡，与主路3卡并行；完整1000 Plan、每条种子、F800、模型、数值与物理协议不变，取消/重试记入回执。资源调整单独登记，S0完成后自动恢复原配置，G/E训练仍为6卡、每卡batch32。原配置文件与绝对截止不改。若核对时41764已经运行，则保留其原3卡作业，不执行取消或替换。

2026-09-10北京时间约10:27，S1评分初始化超时恢复计划（实施前）：CPU评分作业41817在N/U工作进程初始化期间报`scientific worker initialization timed out`，尚未产生完整分数；G1更新、1000条生成与RAW物理标签已经完成。先确认评分作业和调度进程均已退出，保留失败组件、提交回执、调度失败记录与全部输入/标签/配置/参考身份哈希。第一次仅使用原科学源码657985c、同一参考缓存、原7个N/U工作进程与原参数重试CPU评分，采用新作业名并保留原失败目录；不重跑生成或物理标签，不把初始化失败转换成未知质量分数，也不重置18:01绝对截止。若相同初始化超时再次发生，再根据工作进程初始化和资源证据登记一个有限的启动/并行问题修复，评价算法、N/U标准、分母和训练配方保持不变。恢复后继续完整S1及后续两轮。

2026-09-10北京时间约10:47，剩余流程的可用GPU容量调整计划（实施前）：S1 F800的6卡作业41831已等待近10分钟；Slurm显示node99的8卡中有3卡被两个长期作业占用，仅5卡可用，6卡请求预计排到9月19日。保持原12小时截止与科学源码657985c，不修改或取消其它作业。停止本轮调度后，仅在41831仍为PENDING且未产生F输出时取消该请求，以新作业名用5卡运行同一F800。为剩余S1—S3登记独立执行覆盖：单路推理/标注5卡，并行3+2卡；后续五次训练采用4卡、每卡batch48，保持全局源样本批量192。已完成G1的6卡×32配置、检查点和数据原样保留；学习率、损失、KL权重/上限、G4/E8遍历上限、每次训练最多1800秒不变。GPU重新分片会改变rank内样本与随机流划分，不能宣称优化轨迹逐位一致，须在训练回执中报告实际覆盖、停止原因和显存。

时间预留也只作调度校正：原256条线性外推在降至5卡后给E1预留约388分钟，会在已有1000条实测速度可完成后续流程时过早拒绝训练。基于S1生成1342秒/6卡、RAW标注约788秒/6卡，S0 F为3162秒/3卡、token标注约490秒/3卡、E模型376秒/3卡和提案物理标注1437秒/3卡，并加上训练前教师/提案收集、CPU评分及缓冲，登记每个未来完整推理反馈轮预留140分钟、当前E训练后的最终编辑评价预留35分钟，另保留15分钟收尾。剩余时间仍按剩余更新数分配，单次训练上限、遍历上限和绝对截止继续约束；这些是基于已观测耗时的估计，不是完成时长保证，也不根据Stable/SUN成绩增加训练设置。实现仅改调度层的受校验执行覆盖与预留读取，先检查4×48=6×32、并行总卡数及非法覆盖拒绝，再以新调度快照恢复，实际模型与评分命令始终绑定657985c。保留旧请求、原配置/预算哈希与新分配回执。

2026-09-10北京时间约12:18，5卡E后处理预留校正计划（实施前、E1训练前）：完整1000条提案物理标注41911实测5卡1011秒，1008.402秒为标注内部计时；同批模型采集41901用2卡572秒，教师评分41903用30秒CPU。按当前5卡容量估算最终E的模型生成约229秒，加上完整提案标注1011秒、两次CPU评分及数据组装/归档的缓冲，后处理30分钟仍高于约21分钟的模型与物理计算估计，并留额外缓冲；这是执行估计，不作时长保证。旧35分钟预留使E1在当前数据齐备时的训练份额略低于既有180秒最低下限，产生过早停止风险。

本次仅将独立执行覆盖中的`reservations_seconds.editor_tail`从2100改为1800秒，未来完整轮8400秒和收尾900秒、4×48训练分配、剩余更新均分规则、单次最多1800秒、G4/E8上限、学习率与KL规则全部沿用；原BUDGET绝对截止和科学源码保持不变。先保留原覆盖、调度状态和输入/标签/数据/检查点/预算哈希；若调度已因该预算下限退出，归档失败并用同一c3f9d75调度及657985c执行源码恢复。已有完成作业和已生成训练配置不重跑或覆盖；仅在E1配置尚不存在且失败原因吻合时恢复该边界。启用前调用既有覆盖验证器确认只有预留与登记元数据改变，输入身份不变；之后记录实际E1时间上限、真实更新/曝光和完整最终评价。此修正依据已完成5卡运行时间，不依据提案Stable/SUN或E1训练效果选择配置。

## 最新范围：完成当前S1后暂停1000运行，再改进Edit/KEEP（用户追加，实施前）

用户在北京时间约12:20要求：“本轮结束后先停止1000的run，然后看看如何进一步提高edit/keep的效果”，并重新要求SUN和meta SUN有实际增量。当前轮解释为已经完成G1及其RAW/F、正在准备E1的S1；完成E1真实训练、同轮最终提案与KEEP/EDIT评价和归档后暂停，不提交G2、E2、G3、E3。原S0、S1数据与已编译的G2数据保留，1000运行标为用户要求暂停，不能记作三轮更新全部完成。

本次范围调整替代上一条尚未启用的30分钟E后处理预留校正；原执行覆盖仍保持editor_tail=2100秒。旧调度已于04:17:03 UTC因剩余训练份额低于180秒下限退出，E1配置和训练作业均尚不存在，保留这次真实执行失败。增加独立的停止轮次登记，绑定原RUN_SPEC、Plan和BUDGET哈希；调度只计划到round1，并在完成S1归档后写入暂停回执，任何进入round2的路径都被上限阻止。科学执行仍固定657985c，新的调度源码单独部署。

新范围下只剩E1一次权重更新，预留当前E后处理35分钟与收尾15分钟，不再为已取消的S2/S3预留运行或训练份额。E1继续原学习率、KL、4×48、最多8遍和单次最多1800秒；时间上限依据新范围算得1800秒，已有G1配置原样保留。先验证停止范围/哈希非法输入被拒绝、E1预算按唯一剩余更新计算、S1完成后确实退出且不写全三轮完成标记，再恢复。不得改写原18:01:15截止。

S1完成后先做只读诊断，评价重新同时报告Stable、SUN与MSUN。按同一current区分提案供给、接受判断、修改范围、已有Stable/MS保留、N/U变化和实际训练曝光；结合固定G/F试验的失败结果，找出能直接验证的改进方向。后续新的方法改动或尝试另写具体计划，不能用评价标签直接选出的组合冒充自主单次输出，不能把更多物理调用的结果与原单次调用成本混为一谈。

用户随后连续明确后续范围：重点全部放在KEEP/EDIT训练和分析，采用目标为SUN与MSUN实际提高；使用现有proposal与DLM draft，旧256条运行也可纳入；尽量要求KEEP/EDIT没有训练过这些来源时仍有提高，另报告见过来源上的增量。因此S1之后保持G/F与已有评价输入固定，只为KEEP/EDIT训练、诊断和评价安排工作。旧256及当前1000的同源前缀不能跨训练与评估，重复结构去重、不同轮次的有效变体保留在同一来源组；候选起始E权重曾训练过的所有来源也留在训练侧。新训练集、开发集、最终评估集按祖先与约化组成隔离，新候选的最终权重与策略由开发集冻结后再评价最终集。旧试验DEV/FINAL若再用于训练，撤销它们对新尝试的独立验证称谓。新的具体训练协议、数据/来源哈希与算力预算在提交前另行登记。
