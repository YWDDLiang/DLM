# 最后改进阶段：当前证据

分析窗口为 2026-09-10 13:02:01 至 17:02:01 UTC。以下是阶段内进度，未完成的候选不能据此替换上一轮验证有效的方案。

保留上一轮 E3、四候选和增量评分器，改用 `2 * predicted_delta_NS + predicted_delta_NMS` 与 KEEP 的精确零增量比较，已经得到可行性证据。未增加 SUN 接受阈值、MS 增量下限或已知 SUN 手工否决。该策略不重新训练旧权重，使用真实候选后的模型预测；选定输出后重新计算实际 SUN/MSUN。

| 来源 | 请求数 | SUN 相对 KEEP | MSUN 相对 KEEP | Stable 相对 KEEP | EDIT 数 |
|---|---:|---:|---:|---:|---:|
| DEV | 247 | +2 | +14 | 0 | 134 |
| 旧 FINAL 的探索性复核 | 246 | +1 | +19 | +1 | 143 |
| 上一轮 256 条来源的探索性复核 | 256 | +2 | +12 | +1 | 133 |

上述各组均未损失原有 SUN。256 条中的 SUN 为13→15，MSUN为129→141，Stable为20→21；新增两条 SUN（153、156）均真正跨过 Stable 门槛，同时有一条非SUN Stable损失。这里的256条此前已经查看，不能重新称为盲测。该新选择规则和上一轮原终态概率方案是不同配置，不能套用原方案SUN+6的结论。完整得失与文件来源见[零增量 KEEP 比较回执](receipts/RETAINED_GAIN_IDENTITY_RESULT_20260910.json)。

E 内容训练已改为每个 minibatch 真正更新一次，并修正 LoRA 与状态条件模块的学习率分组。KL 作为正则项使用，不再触发停止内容学习。两档学习率各完成 248 次内容更新、248 次头部更新，492 条来源各实际使用 8 遍。加入 86 条经过物理验证的全局教师后，第三个版本完成 296 次内容及头部更新。

一致的原子序列置换同时变换当前结构、目标、提案和位置监督。在相同的 897 条旧提案输入上，第 0 个原子的选择从 810 次下降至 240 或 383 次。加入全局教师的版本生成了 422 条 full-cell 提案和 577 条局部提案；这证明实际编辑范围发生变化，物理效果仍须另行评价。

837 的失败来自晶格角度揭示顺序。条件重采样 gamma 后，在 47 次 DLM 前向内得到合法结构与图。原始结构和 F 后结构均通过物理收敛验证，最终 hull 分别约为 +0.1772 和 +0.1249 eV/atom，因此不能计作 Stable、MS 或 SUN。549 的全晶胞诊断提案仍在弛豫途中出现无效几何，尚未修复。

将 KEEP 本身作为终态模型评分候选后，原损失版本全选 KEEP；平衡 KEEP/EDIT 排序损失后，SUN 获得部分提升，但留出来源的 MSUN 明显下降。联合 NS/NMS 软效用减少了 EDIT 数量，仍未改善留出集整体表现。直接使用保留的原终态评分器评价 KEEP 也几乎全部 KEEP。上述替代版本保留为研究对照。最终增量评分的身份基准为精确的零，不需要再拟合 KEEP 的终态概率，这一差别带来了上表的有效结果。

上一轮有效内容见[保留清单](receipts/PROVEN_COMPONENTS_TO_RETAIN_20260910.json)。数据来源的精确说明见[来源澄清](receipts/TRAIN_SOURCE_CLARIFICATION_20260910.json)：扩展池是 Planner 生成的合成请求，不是 MP20 原始训练表行。正式训练将分开记录来源、原始划分与用途，并排除所有评估组成及其后代。
