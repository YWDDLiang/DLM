# R03 集成执行状态

最新状态（2026-09-07 09:56 UTC）：256 试跑的 R/I 作业 **40580** 正在运行，固定 2 A800、8 CPU。R 组件已完整结束，I 正在精修。生产源码为 `8167ae54f545936ac93005d336358d61b6e85d8d`；它与已通过几何预检的版本保持全部生成模块一致，仅调整评价接口。G/P 作业 **40626** 已提交，复用该版本和同一份 I Plans，依赖 40580 成功后启动，不同时占用超过两张卡。

- I Planner 已完成 256/256，解析和 C3FD 约束均为 256/256；R Planner 也已完成，2 条计划失败保留在 256 分母内。结构生成与固定 model494、tau800 refinement 继续执行。
- 按用户最新要求，仅计算 comp_valid 与 struct_valid，取消新增 Direct 全套指标；SUN 评价仍保留。几何预检 native 的 comp/struct 分别为 I 16/8、G 15/15、P 15/15（分母均为 16）；tau800 为 I 16/16、G 15/15、P 15/14。
- 计算节点不能联网。原查询作业 40544 的失败记录保留；已在现有登录会话完成 canary 官方参考查询，21 个缺失系统全部补齐。pilot256 官方查询及 finalize 也已成功完成（查询耗时 796.45 秒），实际端点覆盖仍在评价时检查。
- R 已完成全流程及两个端点各 256 条标签：native 的 comp_valid 224/256、struct_valid 116/256；tau800 为 222/256、249/256。R 的 249 个可精修结构耗时 33:04，原始分母仍为 256。I 的 native 为 comp_valid 255/256、struct_valid 128/256，native 标签齐全；最新精修进度为 211/252。
- SUN 尚未评分，不能把参考缓存完成或几何有效改善写成 SUN 提升。在 09:05:26 UTC 的预定截止前，已因完整 G/P 采用判据不可用而按预先规定默认冻结为 G，见 `METHOD_FREEZE.json`（本地与远程一致）。后出的 P 比较保留为开发结果，不追溯修改本次冻结决定。
- 按用户要求快速推进、减少测试和重复代码，后续复用已有命令行入口与参数，不再为普通查询和状态检查新增脚本。
- 正式阶段入口已准备：同一 `run_trial.py` 接收样本数、两个独立种子及冻结生产目录，复用原 `run_component.py`；每张卡交叉执行一个 R 和一个候选 seed 组件以减少尾部等待。现有端点合并器和评价器用于每 seed 及整个 cohort 的 N/U/SUN，不相加各 seed 内的 unique 数。正式样本数、种子和预算尚未登记，正式采样尚未开始；入口要求本次 256 评价完成后才能启动。

以下保留执行过程记录；阶段中的“下一步”以以上最新状态为准。

用户已批准执行，并要求过程中主动汇报、常规问题继续处理。

- 开始：2026-09-07 03:35:26 UTC（北京时间11:35:26）。
- 硬截止：2026-09-07 15:35:26 UTC（北京时间23:35:26）。
- 分支：`codex/r03-c3fd-rich-main`。
- 当前：P0/B0/C3FD/model494 资产身份均通过；控制头作业 40395 已成功完成。公式约束桥接、原始 Planner、R03 构造、指针与物理适配共 96 项 CPU 检查通过。
- 核心边界：P0 不重训；不启用独立 Typed Composer/Expander；不重新采集或标注物理训练数据；B0 权重、原始 R03 顺序与 model494 固定。用户于 06:20 UTC 明确追加要求，将先前已验证的几何解码机制用于初始构造。
- 资源上限：用户在 07:00 UTC 后追加“你先按照2卡进行吧”，本任务现固定最多 2 A800、每 GPU 4 CPU、总 8 CPU、最多两个并发作业。正式样本规模在开始正式采样前根据吞吐锁定。
- 作业 40395：1 A800、4 CPU；保留全部 27,136 条训练记录，冻结 P0 后训练指针头一遍。验证集 9,047 条，首元素准确率 78.9433%，完整顺序准确率 70.9517%；权重 SHA256 `bc62f0606c21dcd74911358ba451b464016f2f97363d5a3f7e0be0eceb945266`。这些是控制监督指标，尚未产生本轮 SUN。
- 运行：40398 为每组 16 次请求的 R/I 工程预检（2 A800、8 CPU），从真实 P0 到 B0 再到固定 tau800；独立于开发集和正式主结果。40399 为现有 K4/K8 离线物理适配（1 A800、4 CPU），在任何评估前固定 128 次更新、LR 1e-6、有效批量 16、seed 20260907；仅使用最终 checkpoint，仍依原开发集规则决定是否采用 P。
- 工程问题：40398 的 R 分支在模型采样前被旧 Bash 的空数组 `set -u` 行为终止，I 分支继续收集接口检查数据；不将未开始的 R 计入科学失败。已修复为非空参数数组；保留 40398，下一次以新的 canary_v2 回执/作业目录运行干净的 16/arm 预检。
- 用户最新要求：整个流程通过后先用 256 次采样试跑。因此下一阶段固定为 R/I/G/P 各 256 请求，其中 I/G/P 共用一次新生成的 C3FD Plans；R 为原始 R03 对照。此前开发集 128 的规模由本次用户指示覆盖；不直接启动 1,000 请求正式采样。256 试跑用于开发判断，P 仍依原 G/P 采用规则判定，不当作独立确认性证据。
- 完整接口复测作业 40400（2 A800、8 CPU）已提交；后续先补 G/P 各 16 请求的真实修订及评价闭环，再开始上述 256 试跑。所有训练/生成均通过 5090 的现有 tmux `ssha800` 会话提交，最多两个 Slurm 作业。
- 原 pending 文件保存用户批准前的规格，运行规格另存 `EXECUTION_SPEC.json`；提交回执记录具体作业 ID 与不可变源代码目录。
- 40400、40399、40403 均已结束；40399 的修订专用物理适配完成固定的 128 次更新，未更换构造 B0。40403 的 G/P 各 16 条完成构造、指针修订、native 与 tau800 标签；这表示流程完整，不表示几何有效率 100%。
- 实际 native 几何有效 R 为 6/16，I/G/P 为 8/16。8 条坏构造有 39 对近距离冲突，现有两处 anchor 仅覆盖 6 对，剩余旧冲突导致局部有效改善也被全局门槛撤回。完整证据保存在 `CANARY_GEOMETRY_DIAGNOSIS.md` 和对应 JSON。
- 用户要求先解释低有效率，再明确要求用上旧版几何机制。因此 256 尚未开始，先新增独立的 `canary_geometry`：相同 16 条实际 Plans/程序/随机种子，G/P 构造增加 000/100 周期别名概率合并及 0.5 Å、125 镜像的原子间 PBC 支持。没有迁移旧版高有效率 checkpoint，也不声称其 99.8% 会自动复现。
- 构造约束以 batch 1 逐条执行，无合法支持的请求保持失败并计入分母，不重采样，不对未完成构造执行修订。下一轮 256 的 R/I/G/P 也统一 body batch 1。旧 `REPAIR_CANARY_COMPLETE` 不再能启动试跑，需新几何预检完成并经实际结果检查后生成 `GEOMETRY_CANARY_ACCEPTED`。
- 下一步：完成几何 hook 与失败记账的 CPU 合约检查，部署不可变新版本，提交 G/P 同条件各 16 条复测，然后按用户要求先做每组 256 的开发试跑。
- 几何预检另包含完整 16 条的 `I_batch1_reference`：与新 G/P 一样逐条构造，仍关闭新增几何与修订。它复用相同实际 Plans/种子，是单独的匹配对照，避免把与旧 batch 8 参照的差异全部归因几何约束。
- 部署 18d225f 首次 fetch 遇到 90 秒网络超时，重试已成功；首次 2 卡提交被旧的账户总资源检查拦截，没有提交任何采样。已确认 40430 的工作/命令路径属于 `/jiaosz/hengzhang/experiments/mp20_sft5_checkpoint424_newraw3_20260907_v1` 的其他实验。按用户最新指示，本任务资源检查改为按本 run 的实际路径识别所有所属作业，限 2 GPU/8 CPU/2 作业；无关作业在回执中保留但不占本任务配额。
- 两卡资源下，几何预检、官方参考补齐与后续 RI/GP 试跑按可用任务配额依次执行。
- 几何预检 **40457** 已完成：不可变生产源码 `82050dd8a3f19297d4ae293399b02035c131933a`、2 A800、8 CPU。匹配的 batch 1 对照 I native Direct 为 8/16，新 G/P 均为 15/16；1 条构造无合法支持保留为失败。tau800 Direct 为 I 16/16、G 15/16、P 14/16，不能据 native 几何改善推断最终 SUN 增益。后续统计代码可以独立部署，256 生产继续绑定本次通过预检的同一源码。
- 原始 15 份输出已按字节/SHA 传回，见 `assets/canary_geometry_40457`。`GEOMETRY_CANARY_40457_COMPARISON` 核对了全部配对与距离：G/P 15 个完整构造及失败前缀逐字一致，精确 PBC 与 125 镜像口径一致；构造已修好原来 8 条坏样本中的 7 条，修订保持几何有效性。
- 第 13 条 Co4Dy12Pb 的当前已提交前缀中，同一 X=Y=0 列的 8 个已定 Z 排布占据可插入间隔；剩余 4 个原子的合法 Z 网格均为空，最好最近距离 0.426 Å。这个结果说明当前前缀死路，不证明组成不可行。
- 统计部署的 tar 时钟警告曾覆盖 tmux 开始标记；已通过准确结束 nonce/退出码只读恢复完成状态，并修复读取器与后续 tar 警告处理。CPU 查询首次提交因旧 Slurm 不接受 `--gres=none` 失败、未启动作业；已通过 `--test-only` 确认 normal 队列的 6 CPU 无 GPU 提交可用。
