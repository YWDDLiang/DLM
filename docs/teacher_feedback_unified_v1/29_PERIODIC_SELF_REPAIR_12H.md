# Periodic DLM self-repair: separately registered 12-hour experiment

User authorization: 2026-09-06 06:49 Asia/Shanghai, following the supplied GPT-6 Pro audit. Budget: **2 A800 / 8 CPU, 06:49–18:49**; original K8 and independent evaluation retain **4 A800 / 16 CPU**. Combined limit remains six GPUs and two concurrent jobs. Existing K8 label job 39916 finishes before its two-card allocation transfers. The unsubmitted 237 support check is superseded.

## Scientific change and boundaries

The intervention is offline physical-feedback-assisted self-improvement: `self-generated x -> Q(R(x))`, using the same frozen, consistently verified relaxation protocol. No additional MP20 construction CE is introduced. Existing CE anchors remain separately identified. All outputs remain discrete DLM tokens; no continuous generator or MLIP is added to native inference.

Original K8 checkpoints/results retain their recipe and identity. Formal new-model training starts from the completed final K8 policy. Round0 39892 is only for an engineering check whose temporary weights are discarded. Source branch: codex/periodic-self-repair; local worktree: D:/codex_work/ai4s/DLM_periodic_self_repair.

## Execution order

1. Reuse complete K4 and K8 train-generated paths and their saved verified `final_structure`. Preserve atom identity, fixed composition, program, failed requests, and coverage. A continuous verified terminal does **not** imply its quantized representation is verified; audit encoding, support, and fresh physical values separately before using targets. Split repair validation by composition before checking outcomes. No evaluation terminal becomes a training target.
2. Add explicit periodic pair bias inside the existing Transformer attention, zero-initialized crystal numeric output increments, and task/mask/noise conditioning. Unknown geometry is explicitly unavailable. Pair images use a declared finite shell; arbitrary-basis exact MIC and strict equivariance are not claimed.
3. Train the DLM on its own erroneous inputs and verified terminal targets with a full-cell conditional staging transaction. Preserve scalar conditional sampling, legal supports, periodic aliases, and whole-transaction rollback. Use family-balanced exact/narrow numeric supervision; dense teacher-forced states are training examples, not new independent crystals.
4. Evaluate repair on fixed held-out self-generated starts: native energy, force/stress tails, validity, A, and B, retaining failures and unchanged outputs. Loss decrease alone cannot pass this stage.
5. Only with usable repair evidence and time remaining, train masked generation from composition-balanced, deduplicated self-generated terminal basins. Declare basin weights and student regularization separately from path probabilities; report novelty, uniqueness, coverage and SUN separately.
6. Report the actual completed endpoint by 18:49, with sample counts and limitations. The original pipeline still locks its method for independent evaluation at 13:00 and delivers by 19:19.

## Primary-source verification

- [MaskGXT paper](https://arxiv.org/abs/2606.22866), [official implementation](https://github.com/kiyoung98/MaskGXT): periodic/ordinal numeric training mechanisms.
- [Crystalite](https://arxiv.org/abs/2604.02270): periodic geometry as an additive attention bias.
- [Self-Generated Error Training](https://arxiv.org/abs/2606.17175): own-model corruptions for token editing; crystal physical feedback is our proposed transfer, not a published performance guarantee.

## Live status

See [the complete design and mathematical audit](../periodic_self_repair_v1/DESIGN_AND_AUDIT.md). User clarification: retain the paper's Planner/program and legal-set core; other implementation may adapt or be dropped. Three read-only audits completed and concrete issues corrected. User has authorized immediate use of free cards; 238 is prepared for a two-GPU real-model preflight plus full-K4 target quantization/physics, followed by full K8 preparation. Separate remote experiment directory: grounding/experiments/periodic_self_repair_20260906/.

06:56: branch created; existing labels confirmed to contain terminal structures, avoiding a fresh terminal-collection run. Implementation and CPU checks are in progress. **No new GPU job or trained repair model yet.**
