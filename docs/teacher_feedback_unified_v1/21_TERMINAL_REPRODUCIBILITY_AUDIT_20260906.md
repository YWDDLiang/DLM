# 全池收益集中度与终态可复算性

2026-09-06 02:22 Asia/Shanghai。此记录说明一次实际发现、一次统一验证修订与训练恢复，
不把数值可复算性当成势模型物理正确性的证明。

## 完整首轮与被暂停的teacher

39885完成1h22m15（label脚本4933.27秒）。连同39873，全4096标签为973verified、
1531not_converged、1565invalid_terminal、26generation_failure、1relaxation_energy_increased。
526/1024组成有验证路径，273组多条、253组一条、498组没有验证路径。

原完整teacher在526个验证组成上求解optimal，平均A/B各下降0.219225eV/atom，
KL0.0384263、ESS729.16。但逐组成诊断发现B的正收益96.27%来自一个Gd2组成，
因此该均值不能支持普遍的低能盆地改善。39889正式训练启动后6分45秒被定向停止；
原teacher目录`data/spad_state_teacher_round0_20260906_v1`和该run均写入_INVALIDATED，
保留所有文件，不使用其不完整参数。参考tau80039890未停止。

对应路径13087:0:0：e0=-6.786974、eR=-499.127869eV/atom、终态Fmax=0、
stressmax=0.102318GPa、FIRE93步、N=2、体积161.134Å³。该组成另两条验证路径的
eR约-14.0363eV/atom。此异常不能简单解释成原生DLM稳定性提升。

## 统一审计的证据与边界

39891使用四卡对**全部973个验证终态**做同势单点复算，共三种表示：原终态、
fractional坐标包裹、加固定[0.137,0.271,0.419]后包裹。没有重新采样或重新弛豫。
在看到结果前固定1meV/atom绝对能量容差，检查鲜算原表示与轨迹能量，以及三种等价
表示间的能量跨度。该job29秒完成：972consistent、1inconsistent，无审计软件错误。

Gd2的原表示复算仍为-499.127869，包裹后相同，平移后-499.128876eV/atom；跨度
**0.0010070801eV/atom**，仅略高于0.001阈值。故不能声称找到了485eV的记账错误，
也不能声称这1.007meV差异解释了异常深能量。异常预测在同一势下基本可复现；
这里只能说它未通过统一数值验证要求，物理可靠性仍有明确疑问。

新labels版本`data/spad_state_labels_round0_consistent_20260906_v2`保留全部4096条
记录、原能量、原验证状态与审计数据。该记录改为terminal_consistency_unverified，
不参与teacher；没有删除组成或重新抽样。新验证协议同样用于后续训练标签和评估
标签。235入口对旧参考原生及tau800标签补同一检查，保留旧报告，并核对原能量阈值
Stable/SUN计数不变；验证子集可以改变。原生生成和tau800本身均不重跑。

这是一次训练前的可靠性修订，阈值边界和数据依赖动机须在汇报中披露；不能只展示
修订后的均值，更不能把通过该检查的所有终态称为真实物理极小值。

## 当前可用teacher与训练

新teacher：`data/spad_state_teacher_round0_consistent_20260906_v2`，972验证路径，
526可监督组成、273多候选组成。solver optimal，A/B平均各下降**22.472meV/atom**，
KL0.0097533、ESS752.10。q相对组内均匀参考u的平均总变差2.5613%。

| 逐组成诊断 | A | B |
|---|---:|---:|
| 改善组成 | 175 | 235 |
| 退步组成 | 98 | 38 |
| 单候选、权重无变化组成 | 253 | 253 |
| 最大单组成的正收益占比 | 10.31% | 38.18% |
| 前10%组成的正收益占比 | 77.71% | 96.24% |

137组A/B同时改善，136组至少一项退步。故收益仍不均匀，额外能量偏移也较小；
单候选验证样本拟合和CE更新可能稀释额外偏好。这不是坐标平均target，但学生是否
学习到所需分布变化仍必须实测。K8刷新保留，用于补充同组成内的验证与比较支持。

**39892**从原39853 warmup重新开始正式两遍训练，使用新teacher和干净的首轮
optimizer；先前39889不计作有效训练或可选checkpoint。后续一次K8刷新继续39892
完成后的optimizer/schedule。原生和tau800学生SUN仍未产生，不以teacher值替代。
