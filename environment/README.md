# Environment placeholder

The A800 Conda export and Python dependency lock have not yet been copied.
After the A800 audit, this directory will contain:

```text
environment.yml
requirements-lock.txt
a800-package-notes.md
```

Do not infer package versions from an unrelated local environment.

