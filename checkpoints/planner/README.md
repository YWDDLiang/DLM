# Planner checkpoint placeholder

Expected files include `adapter_model.safetensors` and the tokenizer/config
files required by the Planner base model. A missing Planner checkpoint falls
back to frozen Plans for quick reproduction.

